import InitCronometro from './cronometro.js'

// Mude a cor da tela para azul e depois para vermelho a cada 2s.
// const btnClique = document.querySelector('')

// function changeBackgroundColor(e) {
//     document.documentElement.classList.add('blue');
    
//     setInterval(() => {
//         if (document.documentElement.classList.contains('blue')) {
//             document.documentElement.classList.remove('blue');
//             document.documentElement.classList.add('red');
//         } else {
//             document.documentElement.classList.add('blue');
//             document.documentElement.classList.remove('red');
//         }
//     }, 2000);
// }

// btnClique.addEventListener('click', changeBackgroundColor)

// Crie um cronometro utilizando o setInterval. Deve ser possível
// iniciar, pausar e resetar (duplo clique no pausar).

InitCronometro();